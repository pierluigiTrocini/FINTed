services:
  db_postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
    ports:
      - 5432:5432

#  spring_backend:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    depends_on:
#      - db_postgres
#    ports:
#      - 8080:8080